<%= stylesheet_link_tag "style.css", "data-turbo-track": "reload" %>

<h1>REVIEWS DE PRODUCTOS</h1>

<div class="reviews">
	<span> REVIEWS </span>
	<% ReviewsController.get_reviews(params[:plu]).each do |review| %>
		<div>
			<p> <%= "#{review.reviewer_name}: #{review.review}" %> </p>
			<div class="delete-button"> <%= button_to "❌", "/reviews/index/#{review.plu}/#{review.id}", method: :delete%> </div>
		</div>
	<% end %>
</div>

<div class="new_review">
	<%= form_with method: :post do |form| %>
		<%= form.label :query, "Escribe tu review!" %>
		<br>
		<%= form.label :query, "Nombre: " %>
		<%= form.text_field :name, :type=>"input" %>
		<br>
		<%= form.label :query, "Review: " %>
		<%= form.text_field :review %>

		<%= form.submit "Enviar" %>
	<% end %>
</div>
